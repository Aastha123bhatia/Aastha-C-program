#include<stdio.h>
#include<conio.h>
void swap(int*,int*);
void main()
{
int swap,a,b;
printf("Enter two no");
scanf("%d%d",&a,&b);
swap(&a,&b);
printf("%d%d",a,b);
getch();
}
void swap(int *x,int *y)
{
int z;
z=*x;
*x=*y;
*y=z;
}
